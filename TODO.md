# TODO

- [x] Refactor `app/(public)/team/page.tsx` to server-render the team list (Prisma query) so Google can see profile links in initial HTML.
- [x] Keep the existing client animations/styling minimal or move them behind a client component if needed.




- [x] Add canonical tag (and optionally robots index/follow) to `app/(public)/team/[slug]/page.tsx`.

- [ ] Update `/team` sitemap/API logic only if you decide to introduce `published` for `TeamMember`.
- [ ] Verify build/lint after changes.

